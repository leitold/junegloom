[project]
name = "junegloom"
version = "0.1.0"
description = "June Gloom or how to create pretty pictures from the Potts model"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "matplotlib>=3.11.0",
    "numpy>=2.5.1",
    "palettable>=3.3.3",
    "pyaudio>=0.2.14",
]
